/* Crie uma funcao chamada filtrarPares ques recebe um array de numeros como parametro e retorna um novo array apenas
com os numeros pares || Create a function called filtroPares that receives an array of numbers as a parameter and returns a new array with only the even numbers. */ 

function criargente(name, age, city) {
    return {
        name : name,
        age : age, 
        city : city
    };
}

console.log(criargente("Messi", 37, "Rosario, Argentina"));



