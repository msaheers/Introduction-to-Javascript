/* Escreva uma funcao chamada adicionarPropriedade que receba um objeto e adiciona uma nova propriedade a ele ||
 Write a function called addProperty that takes an object and adds a new property to it. */

function calcularidadeemdias(idade) {
    let dias = idade * 365;
    return dias;
}
console.log(calcularidadeemdias(10));