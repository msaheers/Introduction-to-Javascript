// Parameters vs Arguments 
// Parameter = the name used in the function (like name above).
// Argument = the actual value you pass (like "John").

function add(a, b) {
    return a + b; 
}

add(5, 3); // 5 and 3 are arguments